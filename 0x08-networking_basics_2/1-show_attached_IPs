#!/usr/bin/env bash

# Use 'ip' command to list all active IPv4 addresses
ips=$(ip -4 addr | grep inet | awk '{print $2}' | cut -d'/' -f1)

# Loop through the IPs and display each one
for ip in $ips; do
    echo "$ip"
done

